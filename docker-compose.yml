version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: gerprocess-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: gerprocess
      POSTGRES_PASSWORD: gerprocess_dev_2025
      POSTGRES_DB: gerprocess
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U gerprocess']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
    driver: local
